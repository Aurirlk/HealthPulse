<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.kmbeast.mapper.FollowupRecordMapper">
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO followup_record (task_id, patient_id, content, proof, status, create_time)
        VALUES (#{taskId}, #{patientId}, #{content}, #{proof}, #{status}, #{createTime})
    </insert>
    <select id="queryByTaskId" resultType="cn.kmbeast.pojo.entity.FollowupRecord">
        SELECT * FROM followup_record WHERE task_id = #{taskId} ORDER BY create_time DESC
    </select>
</mapper>
