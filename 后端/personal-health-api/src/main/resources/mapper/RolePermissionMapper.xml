<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.kmbeast.mapper.RolePermissionMapper">
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO role_permission (role_id, permission_id, create_time) VALUES (#{roleId}, #{permissionId}, #{createTime})
    </insert>
    <delete id="deleteByRoleId">DELETE FROM role_permission WHERE role_id = #{roleId}</delete>
    <insert id="batchSave">
        INSERT INTO role_permission (role_id, permission_id, create_time) VALUES
        <foreach collection="list" item="item" separator=",">(#{item.roleId}, #{item.permissionId}, #{item.createTime})</foreach>
    </insert>
</mapper>
