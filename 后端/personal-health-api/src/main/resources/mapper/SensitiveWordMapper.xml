<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.kmbeast.mapper.SensitiveWordMapper">
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sensitive_word (word, status, create_time) VALUES (#{word}, #{status}, #{createTime})
    </insert>
    <delete id="batchDelete">
        DELETE FROM sensitive_word WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </delete>
    <select id="queryAll" resultType="cn.kmbeast.pojo.entity.SensitiveWord">
        SELECT * FROM sensitive_word WHERE status = 1 ORDER BY create_time DESC
    </select>
    <select id="queryAllWords" resultType="string">
        SELECT word FROM sensitive_word WHERE status = 1
    </select>
</mapper>
