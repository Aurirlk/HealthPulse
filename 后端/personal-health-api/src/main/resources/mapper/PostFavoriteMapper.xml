<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.kmbeast.mapper.PostFavoriteMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO post_favorite (user_id, post_id, create_time)
        VALUES (#{userId}, #{postId}, #{createTime})
    </insert>

    <delete id="delete">
        DELETE FROM post_favorite WHERE user_id = #{userId} AND post_id = #{postId}
    </delete>

    <select id="getByUserAndPost" resultType="cn.kmbeast.pojo.entity.PostFavorite">
        SELECT * FROM post_favorite WHERE user_id = #{userId} AND post_id = #{postId}
    </select>
</mapper>
