<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.kmbeast.mapper.OrderItemMapper">
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_item (order_id, product_id, product_name, product_price, product_cover, quantity, subtotal, create_time)
        VALUES (#{orderId}, #{productId}, #{productName}, #{productPrice}, #{productCover}, #{quantity}, #{subtotal}, #{createTime})
    </insert>
    <insert id="batchSave">
        INSERT INTO order_item (order_id, product_id, product_name, product_price, product_cover, quantity, subtotal, create_time) VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.productName}, #{item.productPrice}, #{item.productCover}, #{item.quantity}, #{item.subtotal}, #{item.createTime})
        </foreach>
    </insert>
    <select id="queryByOrderId" resultType="cn.kmbeast.pojo.entity.OrderItem">
        SELECT * FROM order_item WHERE order_id = #{orderId}
    </select>
</mapper>
