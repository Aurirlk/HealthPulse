<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.kmbeast.mapper.UserFollowMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_follow (follower_id, followee_id, create_time)
        VALUES (#{followerId}, #{followeeId}, #{createTime})
    </insert>

    <delete id="delete">
        DELETE FROM user_follow WHERE follower_id = #{followerId} AND followee_id = #{followeeId}
    </delete>

    <select id="getByFollowerAndFollowee" resultType="cn.kmbeast.pojo.entity.UserFollow">
        SELECT * FROM user_follow WHERE follower_id = #{followerId} AND followee_id = #{followeeId}
    </select>

    <select id="getFollowerIds" resultType="integer">
        SELECT follower_id FROM user_follow WHERE followee_id = #{followeeId}
    </select>

    <select id="countFollowers" resultType="integer">
        SELECT COUNT(*) FROM user_follow WHERE followee_id = #{followeeId}
    </select>

    <select id="countFollowing" resultType="integer">
        SELECT COUNT(*) FROM user_follow WHERE follower_id = #{followerId}
    </select>
</mapper>
