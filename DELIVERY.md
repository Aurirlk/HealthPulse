# 智康云健康管理系统 — 用户交付手册

> **版本**: v1.0（2026-07-31）
> **适用系统**: 智康云 · AI 驱动的全栈健康管理平台
> **本文档定位**: 面向部署方、运维方与使用方的交付说明。内部审查与修复过程记录已归档，不在此重复。

---

## 目录

1. [产品简介](#1-产品简介)
2. [系统架构](#2-系统架构)
3. [功能清单](#3-功能清单)
4. [部署指南](#4-部署指南)
5. [配置说明](#5-配置说明)
6. [使用指南](#6-使用指南)
7. [安全说明](#7-安全说明)
8. [已知限制与后续规划](#8-已知限制与后续规划)
9. [故障排查](#9-故障排查)

---

## 1. 产品简介

智康云是一个 **AI 驱动的健康管理平台**，面向个人用户提供智能问诊、健康数据管理、药品服务与知识检索能力：

- **AI 智能问诊**：支持多角色（全科医生、营养师、心理咨询师、报告分析师、全能助手）对话
- **ReAct 智能体**：对话中自动调用药品查询、健康数据、知识检索等工具，5 轮自主推理
- **RAG 知识检索**：从平台健康知识库检索相关文章辅助回答
- **健康数据管理**：血压、血糖、血脂等指标记录与趋势分析
- **药品服务**：药品查询、订阅推荐
- **健康报告**：PDF 体检报告解析与解读
- **联网搜索**：多搜索引擎兜底（可选）
- **语音交互**：浏览器原生语音输入（Chrome/Edge）与语音朗读

## 2. 系统架构

```
┌─────────────────────────────────────────────┐
│  前端 Vue 3（Nginx 静态托管，反向代理 /api）      │
└──────────────────┬──────────────────────────┘
                   │ HTTPS
┌──────────────────▼──────────────────────────┐
│  后端 Spring Boot 2.7（Java 17）              │
│  ├─ JWT 用户鉴权 + CRM API Key（机器接口）       │
│  ├─ ReAct Agent（OpenAI function calling）     │
│  ├─ RAG 知识检索（MySQL LIKE + 本地向量库）      │
│  └─ SSE 流式对话 / WebSocket 通知               │
└──────┬──────────────┬──────────────┬─────────┘
       │              │              │
   MySQL 8.0      SQLite(CRM)     AI 厂商 API
   业务数据         对话历史      (DeepSeek/通义等)
```

**运行时组件**：

| 组件 | 说明 | 默认端口 |
|------|------|----------|
| 前端 | Vue 3 + Nginx | 80 |
| 后端 | Spring Boot 2.7.18 + Java 17 | 21090 |
| MySQL | 业务主库（8.0） | 3306 |
| SQLite | CRM 对话历史（WAL 模式） | 文件 |
| 可选 | 本地医疗大模型（vLLM，端口 8000�� | 8000 |
| 可选 | Neo4j 知识图谱 | 7687 |

## 3. 功能清单

### 3.1 用户端

| 功能 | 说明 |
|------|------|
| 注册/登录 | 手机号 + 密码（BCrypt 加密存储） |
| AI 智能问诊 | 6 种角色；普通/流式两种输出模式；可开启联网搜索、知识库、健康数据接入 |
| 健康数据 | 自定义健康模型；指标录入、趋势图表、JSON 导入导出 |
| 药品管理 | 药品查询、订阅管理、AI 药品推荐 |
| 健康报告 | PDF 体检报告上传 → 自动提取血压/血糖/BMI/心率 → AI 解读 |
| 语音输入 | 按住麦克风说话（Chrome/Edge 原生支持，无需后端） |
| 知识库检索 | 健康文章浏览与 AI 问答引用 |
| 消息通知 | WebSocket 实时推送 |

### 3.2 管理端

| 功能 | 说明 |
|------|------|
| 用户管理 | 用户列表、状态管理 |
| 健康数据管理 | 全量健康记录查看 |
| 药品管理 | 药品 CRUD 与分类 |
| 资讯管理 | 健康文章发布（RAG 知识来源） |
| AI 配置 | 厂商密钥、模型、提示词配置 |
| 系统配置 | 全局参数、横幅通知 |
| RAG 监控 | 知识库检索质量看板（真实评测管线接入前指标置灰） |

### 3.3 CRM 接口（机器对机器）

| 接口 | 说明 | 认证 |
|------|------|------|
| `POST /crm/chat` | 非流式对话 | X-CRM-API-Key |
| `POST /crm/chat/stream` | 流式对话（SSE） | X-CRM-API-Key |
| `GET /crm/history/{phone}` | 指定手机号对话历史 | X-CRM-API-Key |
| `POST /crm/sql/query` | 只读 SQL 查询 | X-CRM-API-Key |
| `GET /crm/health` | 健康检查（免认证） | — |

> 说明：CRM 接口为运营/客服场景预留，`execute_sql` 等工具已强制租户隔离（只能查询当前会话手机号的数据）。

## 4. 部署指南

### 4.1 Docker Compose（推荐）

前置：Docker + Docker Compose；准备好 `.env` 文件（见 5.1）。

```bash
# 1. 准备环境变量（务必设置，缺任一密钥服务会拒绝启动）
cat > .env << 'EOF'
DB_ROOT_PASSWORD=你的MySQL密码
JWT_SECRET=至少32字节随机字符串（openssl rand -base64 48）
CRM_API_KEY=至少24字符随机字符串（openssl rand -hex 32）
EMBEDDING_API_URL=嵌入服务地址（可选，RAG 向量检索用）
AI_API_KEY=DeepSeek等厂商密钥
EOF

# 2. 启动
docker compose up -d --build

# 3. 验证
curl http://localhost:21090/api/personal-health/v1.0/user/login   # 存活探测
```

### 4.2 裸机部署

```bash
# 后端
cd 后端/personal-health-api
mvn package -DskipTests
export JWT_SECRET=... CRM_API_KEY=... DB_PASS=...
java -jar target/*.jar

# 前端
cd 前端/personal-heath-view
npm ci && npm run build   # 产物在 dist/，交给 Nginx 托管
```

### 4.3 关键运维事项

- **镜像/进程以非 root 运行**（后端镜像已内置 appuser）
- **数据卷持久化**：`backend_data`（上传文件）、`mysql_data`、`crm_data`（对话历史）
- **备份清单**：MySQL 全库、`crm_data/chat_history.db`（WAL 模式下需 `sqlite3 .backup` 或使用 `/crm/sqlite/backup` 接口）

## 5. 配置说明

### 5.1 必须配置（缺失即拒绝启动）

| 配置项 | 环境变量 | 说明 |
|--------|----------|------|
| JWT 密钥 | `JWT_SECRET` | ≥32 字节随机串，用于签发登录令牌 |
| CRM API Key | `CRM_API_KEY` | ≥24 字符，保护 `/crm/**` 机器接口 |
| MySQL 密码 | `DB_ROOT_PASSWORD` | 数据库 root 密码 |
| 嵌入服务地址 | `EMBEDDING_API_URL` | 向量检索用（DeepSeek 无此接口，需 OpenAI 兼容或本地服务） |

### 5.2 可选配置

| 配置项 | 环境变量/键 | 默认值 |
|--------|-------------|--------|
| AI 厂商 | `AI_PROVIDER` | deepseek |
| AI 密钥 | `AI_API_KEY` | 空 |
| 文件存储根 | `FILE_STORAGE_ROOT` | `/app/data`（容器） |
| 对外文件 URL | `my-server.public-base-url` | localhost（生产必须改为域名） |
| 嵌入模型 | `EMBEDDING_MODEL` | text-embedding-3-small |
| 健康数据目录 | `HEALTH_DATA_DIR` | ./ai_data/health |

### 5.3 安全配置基线

- 生产环境**必须 HTTPS**（语音输入 getUserMedia 仅 HTTPS 可用）
- 反向代理需把 `/api` 转发到后端 21090、WebSocket `/ws` 到 21091
- 前端 `VUE_APP_API_BASE` 生产建议 `/api/personal-health/v1.0`（相对路径走反代）

## 6. 使用指南

### 6.1 AI 问诊

1. 登录后进入「AI 健康分析」页
2. 选择角色（全科医生/营养师/心理咨询师/报告分析师/全能助手/健康助手）
3. 输入症状或按住麦克风说话
4. 可勾选「联网搜索」「知识库」「健康数据接入」增强回答
5. AI 回答末尾会附相关文章推荐

### 6.2 健康数据

- 「健康数据」页录入指标（血压/血糖/血脂/体重等自定义模型）
- AI 问诊开启「健康数据接入」后，模型可基于你的指标给出个性化建议
- 支持 JSON 导入导出迁移数据

### 6.3 体检报告解读

1. 上传 PDF 体检报告（≤50MB、≤100 页）
2. 系统自动提取血压、血糖、BMI、心率等指标
3. 交由「报告分析师」角色解读

> 注意：自动提取结果请人工核对，异常指标以医院报告为准。

### 6.4 语音

- **输入**：输入框右侧麦克风按钮，按住说话松开识别（Chrome/Edge；Safari/Firefox 提示降级）
- **朗读**：AI 回答支持语音朗读（浏览器内置，不占用服务器资源）

## 7. 安全说明

| 项 | 现状 |
|----|------|
| 密码存储 | BCrypt 加盐哈希 |
| 登录令牌 | JWT（密钥外部注入，启动强校验） |
| 会话越权 | AI 会话/文件上传均有归属校验（IDOR 已修复） |
| CRM 接口 | API Key 认证 + 强制租户隔离（仅查本会话用户数据） |
| SQL 注入 | 只读 SQL 守卫（词法校验 + 关键词黑名单 + 禁子查询） |
| 文件上传 | 登录态 + 扩展名白名单 + 魔数校验 + 随机文件名（不可枚举） |
| XSS | AI 输出经 DOMPurify 白名单净化 |
| 健康数据 | 训练与检索数据不含真实患者 PII；生产建议按等保三级测评 |
| 日志 | logback 滚动归档，异常不回传前端 |

**合规提醒**：平台涉及个人健康信息（敏感个人信息），正式商用前建议完成等保三级测评与个人信息保护影响评估，并对第三方 AI 厂商的数据出境做合规确认。

## 8. 已知限制与后续规划

| 项 | 状态 |
|----|------|
| RAG 质量评测 | 真实 RAGAS 评测管线未接入，监控页指标暂置灰（不再显示模拟数据） |
| 向量库灌数 | 文章 → 分块 → 向量入库的 ingestion 管线未落地，当前知识检索走 MySQL LIKE |
| 服务端 ASR/TTS | 未实现；语音使用浏览器原生能力 |
| API Key 存储 | 密钥由环境变量注入；管理端保存的配置建议后续信封加密 |
| 测试覆盖 | <5%，CI 已移除 continue-on-error，测试补全进行中 |
| 微调模型 | HealthPulse-Qwen2.5-7B（LoRA 微调，Apache-2.0），发布与部署见 `dir/README.md` |

**路线图**：RAG ingestion 管线 → ES/向量混合检索 → RAGAS 评测 → 服务端语音 → 测试覆盖率 ≥50% → Spring Boot 3 升级。

## 9. 故障排查

| 症状 | 排查 |
|------|------|
| 服务拒绝启动 | 检查 JWT_SECRET / CRM_API_KEY / EMBEDDING_API_URL 是否设置且强度达标 |
| 登录后请求 401 | token 过期（默认 7 天）或密钥被更换导致旧 token 失效 |
| AI 无响应 | 检查 AI_API_KEY 与上游厂商余额；后端日志 `logs/error.log` |
| 图片/文件上传失败 | 检查登录态、文件类型（扩展名+内容须一致）、存储目录可写 |
| 语音按钮无反应 | 浏览器须为 Chrome/Edge 且页面须为 HTTPS 或 localhost |
| 上传文件 404 | 容器重启后存储卷未挂载（检查 `FILE_STORAGE_ROOT` 指向持久卷） |

---

*本文档由智康云项目交付流程生成。系统版本变更时请同步更新本文档。*
