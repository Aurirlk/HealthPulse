# 智康云健康管理系统 - 功能测试方案

## 测试环境

- 后端：http://localhost:21090/api/personal-health/v1.0
- 前端：http://localhost:5173
- 数据库：MySQL personal_health
- 测试账号：admin/123456（需先重置密码）

---

## 1. 用户模块测试

### 1.1 登录测试
| 用例 | 操作 | 预期结果 |
|------|------|----------|
| 正常登录 | 输入正确账号密码 | 返回 token，code=200 |
| 密码错误 | 输入错误密码 | 返回"密码错误"，code=400 |
| 账号不存在 | 输入不存在的账号 | 返回"账号不存在"，code=400 |
| 空账号 | 不输入账号 | 返回"账号不能为空"，code=400 |

### 1.2 登录状态测试
| 用例 | 操作 | 预期结果 |
|------|------|----------|
| 禁止登录 | 设置 is_login=1 | 返回"登录状态异常" |
| 允许登录 | 设置 is_login=0 | 正常登录 |

### 1.3 用户信息测试
| 用例 | 操作 | 预期结果 |
|------|------|----------|
| 获取用户信息 | GET /user/auth | 返回用户详情 |
| 更新用户信息 | PUT /user/update | 更新成功 |

---

## 2. 健康指标模块测试

### 2.1 健康模型配置测试
| 用例 | 操作 | 预期结果 |
|------|------|----------|
| 获取全局模型 | GET /health-model-config?isGlobal=1 | 返回5个全局模型 |
| 创建自定义模型 | POST /health-model-config/save | 创建成功 |
| 查询模型列表 | GET /health-model-config | 返回模型列表 |

### 2.2 健康记录测试
| 用例 | 操作 | 预期结果 |
|------|------|----------|
| 保存健康记录 | POST /user-health/save | 保存成功，公共指标同步到画像 |
| 查询健康记录 | GET /user-health | 返回记录列表 |
| 导出健康记录 | GET /user-health/export | 返回JSON数据 |

### 2.3 用户画像测试
| 用例 | 操作 | 预期结果 |
|------|------|----------|
| 获取用户画像 | GET /patient-profile | 返回画像数据 |
| 公共指标同步 | 记录血压/血糖 | 自动更新到 patient_profile |

---

## 3. AI配置模块测试

### 3.1 LLM配置测试
| 用例 | 操作 | 预期结果 |
|------|------|----------|
| 获取AI配置 | GET /ai/config/get | 返回配置信息（隐藏API Key） |
| 切换厂商 | POST /ai/config/switch-provider | 切换成功 |
| 更新配置 | POST /ai/config/update | 更新成功 |
| 重置配置 | POST /ai/config/reset | 恢复默认 |

### 3.2 语音配置测试
| 用例 | 操作 | 预期结果 |
|------|------|----------|
| 获取语音配置 | GET /ai/voice/config/get | 返回ASR/TTS配置 |
| 获取ASR Provider列表 | GET /ai/voice/config/asr/providers | 返回3个Provider |
| 获取TTS Provider列表 | GET /ai/voice/config/tts/providers | 返回3个Provider |
| 更新语音配置 | POST /ai/voice/config/update | 更新成功 |
| 测试ASR | POST /ai/voice/config/asr/test | 返回状态 |
| 测试TTS | POST /ai/voice/config/tts/test | 返回状态 |

---

## 4. AI对话模块测试

### 4.1 基本对话测试
| 用例 | 操作 | 预期结果 |
|------|------|----------|
| 发送消息 | POST /ai/chat | 返回AI回复 |
| 流式对话 | POST /ai/chat/stream | SSE流式返回 |
| 获取会话列表 | GET /ai/conversations | 返回会话列表 |
| 获取历史消息 | GET /ai/conversations/{id}/messages | 返回消息列表 |

### 4.2 AI角色测试
| 用例 | 操作 | 预期结果 |
|------|------|----------|
| 健康助手 | agent_type=consultant | 通用健康咨询 |
| 全科医生 | agent_type=doctor | 专业医疗建议 |
| 营养师 | agent_type=nutritionist | 营养饮食建议 |
| 报告分析师 | agent_type=analyst | 健康报告分析 |

---

## 5. 药品模块测试

### 5.1 药品查询测试
| 用例 | 操作 | 预期结果 |
|------|------|----------|
| 查询药品 | POST /drug/query | 返回药品列表 |
| 药品详情 | GET /drug/{id} | 返回药品详情 |
| 药品分类 | GET /drug/category | 返回分类列表 |

### 5.2 药品订阅测试
| 用例 | 操作 | 预期结果 |
|------|------|----------|
| 订阅药品 | POST /drug/subscribe/{id} | 订阅成功 |
| 取消订阅 | DELETE /drug/subscribe/{id} | 取消成功 |
| 我的订阅 | GET /drug/subscriptions | 返回订阅列表 |

---

## 6. 资讯模块测试

### 6.1 资讯查询测试
| 用例 | 操作 | 预期结果 |
|------|------|----------|
| 资讯列表 | GET /news | 返回资讯列表 |
| 资讯详情 | GET /news/{id} | 返回资讯详情 |
| 轮播图 | GET /news/banner | 返回轮播图列表 |
| 资讯搜索 | GET /news/search | 搜索结果 |

### 6.2 收藏测试
| 用例 | 操作 | 预期结果 |
|------|------|----------|
| 收藏资讯 | POST /news/save/{id} | 收藏成功 |
| 取消收藏 | DELETE /news/save/{id} | 取消成功 |
| 我的收藏 | GET /news/saves | 返回收藏列表 |

---

## 7. 管理后台测试

### 7.1 系统配置测试
| 用例 | 操作 | 预期结果 |
|------|------|----------|
| 获取系统配置 | GET /system/config | 返回配置列表 |
| 更新配置 | POST /system/config/batch-update | 更新成功 |

### 7.2 用户管理测试
| 用例 | 操作 | 预期结果 |
|------|------|----------|
| 用户列表 | GET /user | 返回用户列表 |
| 禁用用户 | PUT /user/update | 禁用成功 |
| 删除用户 | DELETE /user/batch-delete | 删除成功 |

---

## 8. 健康报告测试

### 8.1 PDF生成测试
| 用例 | 操作 | 预期结果 |
|------|------|----------|
| 生成报告 | GET /report/health-pdf | 返回PDF文件 |

---

## 9. 数据库测试

### 9.1 表结构测试
| 用例 | 操作 | 预期结果 |
|------|------|----------|
| 检查表数量 | SHOW TABLES | 15张表 |
| 检查health_model_config | SELECT * | 包含category字段 |
| 检查patient_profile | SELECT * | 画像数据完整 |

### 9.2 数据完整性测试
| 用例 | 操作 | 预期结果 |
|------|------|----------|
| 用户数据 | SELECT COUNT(*) FROM user | 17条记录 |
| 资讯数据 | SELECT COUNT(*) FROM news | 21条记录 |
| 药品数据 | SELECT COUNT(*) FROM drug | 55条记录 |
| AI配置 | SELECT COUNT(*) FROM ai_config | 16条记录 |
| 系统配置 | SELECT COUNT(*) FROM system_config | 24条记录 |

---

## 10. 测试步骤（快速验证）

### 步骤1：重置admin密码
```sql
-- 在Navicat或MySQL命令行执行
UPDATE user SET user_pwd = '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBOsl7iAt6Z5EH' WHERE user_account = 'admin';
UPDATE user SET is_login = 0 WHERE user_account = 'admin';
```

### 步骤2：启动后端
```bash
cd 后端/personal-health-api
mvn spring-boot:run
```

### 步骤3：启动前端
```bash
cd 前端/personal-heath-view
npm run dev
```

### 步骤4：访问前端
- 打开 http://localhost:5173
- 使用 admin/123456 登录
- 测试各功能模块

### 步骤5：测试API接口
```bash
# 测试登录
curl -X POST http://localhost:21090/api/personal-health/v1.0/user/login \
  -H "Content-Type: application/json" \
  -d '{"userAccount":"admin","userPwd":"123456"}'

# 测试获取token后访问其他接口
curl -X GET http://localhost:21090/api/personal-health/v1.0/ai/config/get \
  -H "Authorization: Bearer {token}"
```

---

## 测试结果记录表

| 测试项 | 状态 | 备注 |
|--------|------|------|
| 用户登录 | ☐ | |
| 健康指标 | ☐ | |
| AI配置 | ☐ | |
| 语音配置 | ☐ | |
| 药品查询 | ☐ | |
| 资讯列表 | ☐ | |
| 健康报告 | ☐ | |
