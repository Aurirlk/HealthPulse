# ==================== 敏感配置文件 ====================
# AI配置文件（包含API Key）
**/config/ai-config.json
**/ai-config.json

# 环境变量文件
.env
.env.*
*.env

# 数据库配置（排除本地配置，保留示例）
**/application.yml
**/application-local.yml
**/application-dev.yml
!**/application-example.yml

# ==================== 构建产物 ====================
# Java
target/
*.class
*.jar
*.war

# Node
node_modules/
dist/
.DS_Store

# ==================== 运行时数据 ====================
# 统一缓存目录（包含所有运行时数据）
cache/

# 旧版缓存目录（兼容）
crm_data/
vector_cache/
chat_cache/
chat_backup/
ai_data/
chat_history/

# 图片上传
pic/
uploads/

# 日志
*.log
logs/

# ==================== IDE ====================
.idea/
.vscode/
*.iml
*.swp

# ==================== 大文件 / 模型权重 (ENG-08) ====================
# 以下内容体积大且为二进制，不应进入 Git 仓库。
# 注意：已跟踪的文件需执行 git rm --cached 才会停止跟踪（见交接手册 11.x）
dir/
*.safetensors
*.pt
*.pth
*.onnx
*.gguf
*.tar
*.tar.gz
*.zip
*.7z

# ==================== 密钥与配置 (SEC) ====================
*.pem
*.key
*.keystore
.env.local
.env.production.local

# 后端编译产物
target/
*.class
# WorkBuddy 工作数据（不进仓库）
.workbuddy/
# JVM 崩溃转储（ENG-12）
hs_err_pid*.log
replay_pid*.log
